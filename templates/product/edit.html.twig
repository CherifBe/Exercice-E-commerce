{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
    <h1>Edit Product</h1>
    <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn-primary btn">Retour</a>
    {{ include('product/_form.html.twig') }}

    <div style="display: flex; justify-content: space-between">
        <div>
            <button class="btn-primary btn">{{ button_label|default('Save') }}</button>
        </div>
        <div>
            {{ include('product/_delete_form.html.twig') }}
        </div>
    </div>



{% endblock %}
