{% extends 'base.html.twig' %}

{% block title %}{{ 'my-shop'|trans }} - {{ 'update'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'product.edit.h1'|trans }}</h1>
    <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn-primary btn">{{ 'back'|trans }}</a>
    {{ include('product/_form.html.twig') }}

    <div style="display: flex; justify-content: space-between">
        <div>
            {{ include('product/_delete_form.html.twig') }}
        </div>
    </div>



{% endblock %}
