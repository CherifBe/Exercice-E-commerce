{% extends 'base.html.twig' %}

{% block title %}{{ 'my-shop'|trans }} - {{ 'profile.index.title'|trans }}{% endblock %}

{% block body %}
    <a class="btn-success btn" href="{{ path('app_profile_orders') }}">{{ 'My-Orders'|trans }}</a>
    <h1>{{ 'profile.index.h1'|trans }} {{ user.firstname }} {{ user.lastname }}</h1>
    {{ form(form) }}
{% endblock %}
